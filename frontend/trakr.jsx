// react
const React = require('react');
const ReactDOM = require('react-dom');

// project


document.addEventListener(
  "DOMContentLoaded",
  () => {
    ReactDOM.render(
      <h1>React lies here</h1>,
       document.getElementById('react-root')
     );
   }
)
